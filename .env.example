
# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Supabase Service Role Key (Keep this secret! Do not expose in the browser)
# Used for server-side operations that require bypassing RLS.
SUPABASE_SERVICE_ROLE_KEY=""

# Stripe Configuration
# Find these in your Stripe dashboard > Developers > API keys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""
STRIPE_SECRET_KEY="" # Keep this secret!

# Stripe Webhook Secret (Keep this secret!)
# Used to verify webhook events sent from Stripe.
STRIPE_WEBHOOK_SECRET=""

# Add any other environment variables your application needs below
# EXAMPLE_VARIABLE=""
